# 01_upload_and_gallery

## Objective
Upload flow with client-side resize/compress, id generation, and atomic commit of image + JSON metadata. Gallery view that fetches all entries under /images/, with tag filter, basic client-side search, and direct-link copy.

## Done When
User can upload an image, see it appear in the gallery, filter by tag, and copy a working direct link.
