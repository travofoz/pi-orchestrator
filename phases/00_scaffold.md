# 00_scaffold

## Objective
Scaffold SvelteKit + Vite with static adapter, DaisyUI + Tailwind, GitHub Pages deploy workflow, Connect GitHub flow (PAT persistence in localStorage), and Octokit client wrapper module.

## Done When
App loads, accepts a PAT, and can list the contents of /images/ in the configured repo.
